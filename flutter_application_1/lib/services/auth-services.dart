//import 'dart:html';

//import 'package:firebase_auth/firebase_auth.dart';
//import 'package:google_sign_in/google_sign_in.dart';

//class AuthService {
  //google sign in
  //signInWithGoole() async {
    // begin the interactive signing process
    //final GoogleSignInAccount? gWoeker = await GoogleSignIn().signIn();

    // obtain auth details from request
    //final GoogleSignInAuthentication gauth = await gWoeker!.authentication;

    // create a new credential for worker
    //final credential = GoogleAuthProvider.credential(
      //accessToken: gauth.accessToken,
      //idToken: gauth.idToken,
    //);

    // sign him in
    //return await FirebaseAuth.instance.signInWithCredential(credential);
  //}
//}
